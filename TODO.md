# Ruiz TODO

## AI 流式输出与任务进度

- [x] 在资料库中实时显示当前导入阶段和阶段进度。
- [x] 将 Chat Completions 接入 SSE 流式响应，并兼容不支持流式输出的 OpenAI 兼容服务。
- [x] 在导入页增加可展开的 AI 任务面板，实时显示“去噪、归并材料、提取知识、整理蓝图、生成卡片”等阶段。
- [ ] 在任务面板中显示当前请求编号、模型输出增量和累计耗时。
- [x] 支持取消仍在运行的导入任务，并保证取消后不会写入半成品数据。
- [x] 流式内容只作为临时诊断视图，最终仍使用完整 JSON 校验结果写入数据库。
- [ ] 对日志和流式视图中的敏感字段执行统一脱敏，并提供清理诊断日志的入口。
